{
  "config": {
    "step": {
      "user": {
        "title": "HA Graph Explorer",
        "description": "Enables server-side storage for the graph panel (token, base URL, layout) at `/api/ha_graph_explorer/config`. Add the Web Page card pointing to `/local/ha-graph/ha-graph.html` if you have not already."
      }
    },
    "abort": {
      "already_configured": "Already configured. Only one instance is allowed."
    }
  }
}
